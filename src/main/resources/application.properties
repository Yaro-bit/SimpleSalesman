# === Production Configuration for SimpleSalesman ===

# --- Database Configuration ---
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}  # Uses DATABASE_URL provided by Railway
spring.datasource.username=${PGUSER:postgres}  # Uses PGUSER provided by Railway
spring.datasource.password=${PGPASSWORD:kEfZqLKmQQGDOkIWgYbHNTVUUnZHuclC}  # Uses PGPASSWORD provided by Railway

# --- OAuth2 Resource Server Configuration ---
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:https://keycloak-production-6fbe.up.railway.app/realms/simple-salesman-backend}  # Uses KEYCLOAK_ISSUER_URI if defined, fallback to default

# --- Server Configuration ---
server.port=8081
# --- Keycloak Configuration ---
simplesalesman.keycloak.base-url=${KEYCLOAK_BASE_URL:https://keycloak-production-6fbe.up.railway.app}  # Uses KEYCLOAK_BASE_URL from environment
simplesalesman.keycloak.realm=simple-salesman-backend  # Keycloak Realm (static)
simplesalesman.keycloak.client-id=${KEYCLOAK_CLIENT_ID:frontend-app}  # Uses KEYCLOAK_CLIENT_ID if defined, fallback to default

# --- Application Debugging ---
simplesalesman.debug=false  # Set to true only for debugging purposes
